{     
  "name": "stan-web-app",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "prod": "turbo run prod"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "pnpm": "^8.x"
  }
}